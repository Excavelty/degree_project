import {InputHandler} from 'inputHandler.js'

class CameraHandler
{
    constructor(camera)
    {
        this.camera = camera;
        this.inputHandler = new InputHandler();
    }

    function update()
    {
        const key = this.inputHandler.keyPressed();

        switch(button)
        {
            case 
        }
    }
}

export default {CameraHandler};